import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    padding: 24,
  },
  title: {
    fontSize: 32,
    color: "black",
    fontFamily: "ubuntu-bold",
    marginTop: 42,
  },
});
